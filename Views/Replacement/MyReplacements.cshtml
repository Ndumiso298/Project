@* @model IEnumerable<Project.Models.ReplacementRequest>
@using Project.Utility

@{
    ViewData["Title"] = "My Replacement Requests";
}

<h2 class="text-primary mb-4">My Replacement Requests</h2>

<table class="table table-striped table-hover align-middle">
    <thead>
        <tr>
            <th>ID</th>
            <th>Date</th>
            <th>Fridge Serial</th>
            <th>Status</th>
            <th>Reason</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var r in Model)
        {
            <tr>
                <td>@r.ReplacementRequestId</td>
                <td>@r.RequestDate.ToString("yyyy-MM-dd")</td>
                <td>@r.FridgeInStock?.FridgeNo</td>
                <td>
                    <span class="badge
                            @(r.Status == SD.Approved ? "bg-success" :
                                                    r.Status == SD.Rejected ? "bg-danger" :
                                                    "bg-secondary")">@r.Status</span>
            </td>
            <td>
                @if (r.Status == SD.Rejected)
                    {
                        <span class="text-danger">@r.Reason</span>
                    }
                    else
                    {
                        <span>@r.Reason</span>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
 *@