@* @model Project.Models.ReplacementRequest
@using Project.Utility

@{
    ViewData["Title"] = "Request Replacement";
}

<div class="container mt-4">
    <h2 class="text-primary mb-4">Request Replacement</h2>

    <div class="card shadow-sm border-0 rounded-3">
        <div class="card-body">
            <div class="mb-4">
                <h5 class="fw-semibold">Fridge Information</h5>
                <p><strong>Model:</strong> @ViewBag.FridgeModel</p>
                <p><strong>Serial Number:</strong> @ViewBag.SerialNumber</p>
            </div>

            <form asp-action="Create" method="post">
                <input type="hidden" asp-for="CustomerId" />
                <input type="hidden" asp-for="FridgeInStockId" />

                <div class="mb-3">
                    <label asp-for="Reason" class="form-label fw-semibold">
                        Reason for Replacement
                    </label>
                    <textarea asp-for="Reason" class="form-control" rows="3" required></textarea>
                    <span asp-validation-for="Reason" class="text-danger"></span>
                </div>

                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary w-50">Submit Request</button>
                    <a asp-controller="Request" asp-action="Index" class="btn btn-outline-dark w-50">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
 *@